import { PrismaClient } from '@prisma/client'; import { cookies } from 'next/headers'; import { redirect } from 'next/navigation'; import Link from 'next/link'; export const dynamic = 'force-dynamic'; const prisma = new PrismaClient(); export default async function CustomersPage() { const cookieStore = cookies(); const role = cookieStore.get('auth_role')?.value; if (role !== 'sales' && role !== 'SUPER_ADMIN') { redirect('/'); } const customers = await prisma.company.findMany({ orderBy: { createdAt: 'desc' }, include: { contacts: true, opportunities: true } }); return ( <div className="min-h-screen bg-gray-50 p-8"> <header className="mb-8 flex justify-between items-center bg-white p-6 rounded-xl shadow-sm border border-gray-100"> <div> <h1 className="text-2xl font-bold text-gray-800 tracking-tight">👥 客户管理中心</h1> <p className="text-sm text-gray-500 mt-1">管理 ERDI TECH 所有的 ED 编号客户</p> </div> <Link href="/dashboard" className="bg-gray-800 text-white px-4 py-2 rounded-lg font-bold hover:bg-gray-700 transition-all"> 返回看板 </Link> </header> <div className="bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden"> <table className="w-full text-left border-collapse"> <thead> <tr className="bg-gray-50 border-b border-gray-100"> <th className="p-4 font-bold text-gray-600 text-sm">客户编号</th> <th className="p-4 font-bold text-gray-600 text-sm">公司名称</th> <th className="p-4 font-bold text-gray-600 text-sm">主要联系人</th> <th className="p-4 font-bold text-gray-600 text-sm">邮箱</th> <th className="p-4 font-bold text-gray-600 text-sm">商机数</th> <th className="p-4 font-bold text-gray-600 text-sm">操作</th> </tr> </thead> <tbody> {customers.map((c) => ( <tr key={c.id} className="border-b border-gray-50 hover:bg-gray-50/50 transition-colors"> <td className="p-4"><span className="bg-blue-50 text-blue-700 px-2 py-1 rounded font-mono text-sm font-bold">{c.customerCode || '未分配'}</span></td> <td className="p-4 font-bold text-gray-800">{c.name}</td> <td className="p-4 text-gray-600">{c.contacts[0]?.firstName || '-'}</td> <td className="p-4 text-gray-500 text-sm">{c.contacts[0]?.email || '-'}</td> <td className="p-4"><span className="bg-green-50 text-green-700 px-2 py-1 rounded text-xs font-bold">{c.opportunities.length} 个商机</span></td> <td className="p-4"> <Link href={`/opportunity?companyId=${c.id}`} className="text-blue-600 hover:underline text-sm font-medium">查看详情</Link> </td> </tr> ))} </tbody> </table> {customers.length === 0 && ( <div className="p-20 text-center text-gray-400"> 📭 暂无客户数据，请先同步 Gmail 客户。 </div> )} </div> </div> ); }
